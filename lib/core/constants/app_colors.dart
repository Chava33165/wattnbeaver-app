import 'package:flutter/material.dart';

class AppColors {
  // Energy (Green)
  static const Color energyPrimary = Color(0xFF34C759);
  static const Color energyLight = Color(0xFF8FE09F);
  static const Color energyDark = Color(0xFF248A3D);

  // Water (Blue)
  static const Color waterPrimary = Color(0xFF007AFF);
  static const Color waterLight = Color(0xFF5AC8FA);
  static const Color waterDark = Color(0xFF0051D5);

  // Alerts
  static const Color alertRed = Color(0xFFFF3B30);

  // Gamification
  static const Color gamificationPurple = Color(0xFFAF52DE);

  // Accents
  static const Color accentOrange = Color(0xFFFF9500);
  static const Color accentYellow = Color(0xFFFFCC00);

  // Backgrounds
  static const Color backgroundPrimary = Color(0xFFFFFFFF);
  static const Color backgroundSecondary = Color(0xFFF2F2F7);
  static const Color backgroundTertiary = Color(0xFFE5E5EA);

  // Dark mode backgrounds
  static const Color darkPrimary = Color(0xFF000000);
  static const Color darkSecondary = Color(0xFF1C1C1E);
  static const Color darkTertiary = Color(0xFF2C2C2E);

  // Text
  static const Color textPrimary = Color(0xFF000000);
  static const Color textSecondary = Color(0xFF3C3C43);
  static const Color textTertiary = Color(0xFF8E8E93);

  // Gradients
  static const List<Color> energyGradient = [
    Color(0xFF34C759),
    Color(0xFF30D158),
  ];

  static const List<Color> waterGradient = [
    Color(0xFF007AFF),
    Color(0xFF5AC8FA),
  ];

  static const List<Color> gamificationGradient = [
    Color(0xFFAF52DE),
    Color(0xFFFF2D55),
  ];
}
